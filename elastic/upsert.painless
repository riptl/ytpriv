if (params.crawled_at <= ctx._source.crawled_at) {
  ctx.op = "noop";
  return;
}
if (params.created_before < ctx._source.created_before) {
  ctx._source.created_before = params.created_before;
}
if (params.created_after > ctx._source.created_after) {
  ctx._source.created_after = params.created_after;
}
ctx._source.likes = params.likes;
ctx._source.replies = params.replies;
